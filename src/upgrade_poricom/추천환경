PyQt 때문에 도커로 하기에 무리가 있다. 따라서 ubuntu:20.04 anaconda 환경에서 이 프로그램을 실행하는 것을 추천한다.
그리고 만약 xcb 관련 오류가 난다면 sudo apt update && sudo apt install libxcb-xinerama0 libx11-xcb1
PyQt 라이브러리 관련 오류라 터미널에서 위의 명령어를 실행하면 된다.
